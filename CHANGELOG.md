# 更新日志

## 1.3.0 - 2026-07-16

- 使用单线程后台存储队列执行 SQLite 操作，减少消息处理阻塞。
- 新增索引化去重摘要，避免按多列扫描近期历史记录。
- 新增 `/tk风险` 及可选的自动风险提醒。
- 新增 `/tk状态`、`/tk审计`、`/tk备份` 和 `/tk导出`。
- 新增普通查询冷却、记录保留期限、审计保留期限、导出上限和生成文件保留数量配置。
- `/tk确认清空` 默认先创建完整数据库备份，备份失败时取消清空。
- 数据库结构升级至 v3，兼容 1.1.0、1.2.0 和旧版 JSON 数据。

## 1.2.0 - 2026-07-15

- 使用 `unified_msg_origin` 隔离不同群聊和会话的数据。
- 增加机器人与服务器名称映射及服务器筛选。
- 根据消息 ID 或时间窗口拦截重复播报。
- 新增玩家汇总表，排行榜无需扫描全部历史记录。
- 新增 `/tk统计` 阶段统计和常见原因排行。
- 新增 `/tk删除` 单条记录删除及管理员操作审计。
- `/tk清空全部` 改为 30 秒二次确认。
- 新增静默模式、查询条数、排行人数和统计天数配置。
- 支持从 1.1.0 SQLite 数据库原地升级。

## 1.1.0 - 2026-07-15

- 将全量 JSON 存储替换为轻量 SQLite 增量存储。
- 增加索引化查询、排行和删除操作。
- 支持旧版 `records.json` 一次性自动迁移。
- 使用 AstrBot 官方插件数据目录，并兼容旧版 AstrBot。
- 精简帮助页和回复文案。

## 1.0.1 - 2026-03-16

- 新增 `/tk排行` 指令。
